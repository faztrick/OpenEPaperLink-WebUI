#ifdef USE_WIFI_MANAGER
// advanced_wifi.cpp deprecated: legacy external WiFiManager-based config portal removed.
// If an advanced captive portal is required in future, implement it via the unified WiFiModule
// and web UI endpoints instead of relying on the external WiFiManager library.

void setupAdvancedWiFi() {}
#endif
