import { Layout } from '../components/Layout';
export default function BuildPage(){
  return (<Layout title="Build" description="Build status (placeholder)"><h2>Build</h2><p>Build status & controls migration pending.</p></Layout>);
}
